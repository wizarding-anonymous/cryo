NODE_ENV=development
PORT=3008

# Postgres (use either DATABASE_URL or discrete vars)
# DATABASE_URL=postgres://user:password@localhost:5432/security_service
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=security_service

# Redis (use either REDIS_URL or discrete vars)
# REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT (choose one)
# JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
JWT_SECRET=dev-secret
