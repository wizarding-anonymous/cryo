<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Т-Банк - Оплата</title>
    <style>
        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            margin: 0; 
            padding: 0; 
            background: linear-gradient(135deg, #ffdd2d, #ffcc02); 
            min-height: 100vh; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
        }
        .container { 
            background: white; 
            padding: 40px; 
            border-radius: 16px; 
            box-shadow: 0 12px 40px rgba(0,0,0,0.15); 
            max-width: 500px; 
            width: 90%; 
        }
        .header { 
            text-align: center; 
            margin-bottom: 30px; 
            border-bottom: 3px solid #ffcc02; 
            padding-bottom: 20px; 
        }
        .logo { 
            color: #333; 
            font-size: 32px; 
            font-weight: 900; 
            margin-bottom: 10px; 
            letter-spacing: -1px; 
        }
        .mock-badge { 
            background: #ff9800; 
            color: white; 
            padding: 6px 14px; 
            border-radius: 20px; 
            font-size: 12px; 
            font-weight: bold; 
            display: inline-block; 
            margin-bottom: 15px; 
        }
        .payment-info { 
            background: linear-gradient(135deg, #fff9c4, #fff3a0); 
            padding: 25px; 
            border-radius: 12px; 
            margin-bottom: 25px; 
            border: 2px solid #ffcc02; 
        }
        .info-row { 
            display: flex; 
            justify-content: space-between; 
            margin-bottom: 12px; 
        }
        .info-label { 
            color: #666; 
            font-weight: 600; 
        }
        .info-value { 
            font-weight: bold; 
            color: #333; 
        }
        .amount { 
            font-size: 28px; 
            color: #ff6b00; 
            font-weight: 900; 
        }
        .payment-options { 
            margin-bottom: 25px; 
        }
        .option-title { 
            font-weight: bold; 
            margin-bottom: 15px; 
            color: #333; 
            font-size: 16px; 
        }
        .option-card { 
            display: flex; 
            align-items: center; 
            padding: 18px; 
            border: 2px solid #e0e0e0; 
            border-radius: 12px; 
            margin-bottom: 12px; 
            cursor: pointer; 
            transition: all 0.3s ease; 
            background: white; 
        }
        .option-card:hover { 
            border-color: #ffcc02; 
            background: #fff9c4; 
            transform: translateY(-2px); 
            box-shadow: 0 4px 12px rgba(255, 204, 2, 0.2); 
        }
        .option-card.selected { 
            border-color: #ffcc02; 
            background: #fff9c4; 
            box-shadow: 0 4px 12px rgba(255, 204, 2, 0.3); 
        }
        .option-radio { 
            margin-right: 15px; 
            transform: scale(1.2); 
        }
        .option-icon { 
            font-size: 24px; 
            margin-right: 12px; 
        }
        .option-text { 
            flex: 1; 
        }
        .option-name { 
            font-weight: bold; 
            color: #333; 
        }
        .option-desc { 
            font-size: 12px; 
            color: #666; 
            margin-top: 2px; 
        }
        .button-group { 
            display: flex; 
            gap: 15px; 
            margin-top: 30px; 
        }
        .btn { 
            flex: 1; 
            padding: 18px; 
            border: none; 
            border-radius: 12px; 
            font-size: 16px; 
            font-weight: bold; 
            cursor: pointer; 
            transition: all 0.3s ease; 
            text-transform: uppercase; 
            letter-spacing: 0.5px; 
        }
        .btn-success { 
            background: linear-gradient(135deg, #ffcc02, #ffdd2d); 
            color: #333; 
            box-shadow: 0 4px 15px rgba(255, 204, 2, 0.4); 
        }
        .btn-success:hover { 
            background: linear-gradient(135deg, #ffb300, #ffcc02); 
            transform: translateY(-2px); 
            box-shadow: 0 6px 20px rgba(255, 204, 2, 0.6); 
        }
        .btn-cancel { 
            background: #6c757d; 
            color: white; 
        }
        .btn-cancel:hover { 
            background: #5a6268; 
            transform: translateY(-2px); 
        }
        .security-info { 
            text-align: center; 
            margin-top: 25px; 
            font-size: 12px; 
            color: #666; 
            line-height: 1.4; 
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="mock-badge">ТЕСТОВЫЙ РЕЖИМ</div>
            <div class="logo">Т—Банк</div>
            <p>Современные платежные решения</p>
        </div>

        <div class="payment-info">
            <div class="info-row">
                <span class="info-label">Номер заказа:</span>
                <span class="info-value">{{orderId}}</span>
            </div>
            <div class="info-row">
                <span class="info-label">Продавец:</span>
                <span class="info-value">Игровая платформа</span>
            </div>
            <div class="info-row">
                <span class="info-label">Сумма платежа:</span>
                <span class="info-value amount">{{amount}} ₽</span>
            </div>
        </div>

        <form id="paymentForm">
            <input type="hidden" name="externalId" value="{{externalId}}">
            
            <div class="payment-options">
                <div class="option-title">Способ оплаты:</div>
                
                <div class="option-card selected" onclick="selectOption(this)">
                    <input type="radio" name="paymentOption" value="card" class="option-radio" checked>
                    <span class="option-icon">💳</span>
                    <div class="option-text">
                        <div class="option-name">Банковская карта</div>
                        <div class="option-desc">Visa, MasterCard, МИР</div>
                    </div>
                </div>
                
                <div class="option-card" onclick="selectOption(this)">
                    <input type="radio" name="paymentOption" value="sbp" class="option-radio">
                    <span class="option-icon">📱</span>
                    <div class="option-text">
                        <div class="option-name">Система быстрых платежей</div>
                        <div class="option-desc">Оплата по QR-коду</div>
                    </div>
                </div>
                
                <div class="option-card" onclick="selectOption(this)">
                    <input type="radio" name="paymentOption" value="installment" class="option-radio">
                    <span class="option-icon">📊</span>
                    <div class="option-text">
                        <div class="option-name">Рассрочка 0%</div>
                        <div class="option-desc">До 12 месяцев без переплат</div>
                    </div>
                </div>
            </div>

            <div class="button-group">
                <button type="button" class="btn btn-success" onclick="confirmPayment()">
                    Оплатить {{amount}} ₽
                </button>
                <button type="button" class="btn btn-cancel" onclick="cancelPayment()">
                    Отмена
                </button>
            </div>
        </form>

        <div class="security-info">
            🔒 Защищено технологией 3-D Secure 2.0<br>
            🛡️ Сертификат безопасности PCI DSS Level 1<br>
            Это имитация платежной системы для тестирования
        </div>
    </div>

    <script>
        function selectOption(element) {
            // Remove selected class from all options
            document.querySelectorAll('.option-card').forEach(option => {
                option.classList.remove('selected');
            });
            
            // Add selected class to clicked option
            element.classList.add('selected');
            
            // Check the radio button
            element.querySelector('input[type="radio"]').checked = true;
        }

        function confirmPayment() {
            const selectedOption = document.querySelector('input[name="paymentOption"]:checked').value;
            
            fetch('{{actionUrl}}', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    action: 'confirm',
                    externalId: '{{externalId}}',
                    paymentOption: selectedOption
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.redirect) {
                    window.location.href = data.redirect;
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('Произошла ошибка при обработке платежа');
            });
        }

        function cancelPayment() {
            fetch('{{actionUrl}}', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    action: 'cancel',
                    externalId: '{{externalId}}'
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.redirect) {
                    window.location.href = data.redirect;
                }
            })
            .catch(error => {
                console.error('Error:', error);
                window.location.href = '/mock/payment/failure/{{paymentId}}';
            });
        }
    </script>
</body>
</html>