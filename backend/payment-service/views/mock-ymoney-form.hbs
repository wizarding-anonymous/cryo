<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ЮMoney - Оплата</title>
    <style>
        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            margin: 0; 
            padding: 0; 
            background: linear-gradient(135deg, #ffdb4d, #ffc107); 
            min-height: 100vh; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
        }
        .container { 
            background: white; 
            padding: 40px; 
            border-radius: 12px; 
            box-shadow: 0 8px 32px rgba(0,0,0,0.1); 
            max-width: 500px; 
            width: 90%; 
        }
        .header { 
            text-align: center; 
            margin-bottom: 30px; 
            border-bottom: 2px solid #ffc107; 
            padding-bottom: 20px; 
        }
        .logo { 
            color: #ffc107; 
            font-size: 28px; 
            font-weight: bold; 
            margin-bottom: 10px; 
        }
        .mock-badge { 
            background: #ff9800; 
            color: white; 
            padding: 5px 12px; 
            border-radius: 15px; 
            font-size: 12px; 
            display: inline-block; 
            margin-bottom: 15px; 
        }
        .payment-info { 
            background: #fff8e1; 
            padding: 20px; 
            border-radius: 8px; 
            margin-bottom: 25px; 
            border-left: 4px solid #ffc107; 
        }
        .info-row { 
            display: flex; 
            justify-content: space-between; 
            margin-bottom: 10px; 
        }
        .info-label { 
            color: #666; 
            font-weight: 500; 
        }
        .info-value { 
            font-weight: bold; 
            color: #333; 
        }
        .amount { 
            font-size: 24px; 
            color: #f57c00; 
        }
        .payment-methods { 
            margin-bottom: 25px; 
        }
        .method-title { 
            font-weight: bold; 
            margin-bottom: 15px; 
            color: #333; 
        }
        .method-option { 
            display: flex; 
            align-items: center; 
            padding: 15px; 
            border: 2px solid #e0e0e0; 
            border-radius: 8px; 
            margin-bottom: 10px; 
            cursor: pointer; 
            transition: all 0.3s; 
        }
        .method-option:hover { 
            border-color: #ffc107; 
            background: #fff8e1; 
        }
        .method-option.selected { 
            border-color: #ffc107; 
            background: #fff8e1; 
        }
        .method-radio { 
            margin-right: 12px; 
        }
        .method-icon { 
            font-size: 20px; 
            margin-right: 10px; 
        }
        .button-group { 
            display: flex; 
            gap: 15px; 
            margin-top: 30px; 
        }
        .btn { 
            flex: 1; 
            padding: 15px; 
            border: none; 
            border-radius: 8px; 
            font-size: 16px; 
            font-weight: bold; 
            cursor: pointer; 
            transition: all 0.3s; 
        }
        .btn-success { 
            background: #ffc107; 
            color: #333; 
        }
        .btn-success:hover { 
            background: #ffb300; 
        }
        .btn-cancel { 
            background: #6c757d; 
            color: white; 
        }
        .btn-cancel:hover { 
            background: #5a6268; 
        }
        .security-info { 
            text-align: center; 
            margin-top: 20px; 
            font-size: 12px; 
            color: #666; 
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="mock-badge">ТЕСТОВЫЙ РЕЖИМ</div>
            <div class="logo">💰 ЮMoney</div>
            <p>Быстрая и безопасная оплата</p>
        </div>

        <div class="payment-info">
            <div class="info-row">
                <span class="info-label">Заказ:</span>
                <span class="info-value">{{orderId}}</span>
            </div>
            <div class="info-row">
                <span class="info-label">Магазин:</span>
                <span class="info-value">Игровая платформа</span>
            </div>
            <div class="info-row">
                <span class="info-label">К оплате:</span>
                <span class="info-value amount">{{amount}} ₽</span>
            </div>
        </div>

        <form id="paymentForm">
            <input type="hidden" name="externalId" value="{{externalId}}">
            
            <div class="payment-methods">
                <div class="method-title">Выберите способ оплаты:</div>
                
                <div class="method-option selected" onclick="selectMethod(this)">
                    <input type="radio" name="paymentMethod" value="wallet" class="method-radio" checked>
                    <span class="method-icon">💳</span>
                    <span>Кошелек ЮMoney</span>
                </div>
                
                <div class="method-option" onclick="selectMethod(this)">
                    <input type="radio" name="paymentMethod" value="card" class="method-radio">
                    <span class="method-icon">💳</span>
                    <span>Банковская карта</span>
                </div>
                
                <div class="method-option" onclick="selectMethod(this)">
                    <input type="radio" name="paymentMethod" value="sberbank" class="method-radio">
                    <span class="method-icon">🏦</span>
                    <span>Сбербанк Онлайн</span>
                </div>
            </div>

            <div class="button-group">
                <button type="button" class="btn btn-success" onclick="confirmPayment()">
                    Оплатить {{amount}} ₽
                </button>
                <button type="button" class="btn btn-cancel" onclick="cancelPayment()">
                    Отменить
                </button>
            </div>
        </form>

        <div class="security-info">
            🔒 Платеж защищен технологией 3-D Secure<br>
            Это имитация платежной системы для тестирования
        </div>
    </div>

    <script>
        function selectMethod(element) {
            // Remove selected class from all options
            document.querySelectorAll('.method-option').forEach(option => {
                option.classList.remove('selected');
            });
            
            // Add selected class to clicked option
            element.classList.add('selected');
            
            // Check the radio button
            element.querySelector('input[type="radio"]').checked = true;
        }

        function confirmPayment() {
            const selectedMethod = document.querySelector('input[name="paymentMethod"]:checked').value;
            
            fetch('{{actionUrl}}', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    action: 'confirm',
                    externalId: '{{externalId}}',
                    paymentMethod: selectedMethod
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.redirect) {
                    window.location.href = data.redirect;
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('Произошла ошибка при обработке платежа');
            });
        }

        function cancelPayment() {
            fetch('{{actionUrl}}', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    action: 'cancel',
                    externalId: '{{externalId}}'
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.redirect) {
                    window.location.href = data.redirect;
                }
            })
            .catch(error => {
                console.error('Error:', error);
                window.location.href = '/mock/payment/failure/{{paymentId}}';
            });
        }
    </script>
</body>
</html>